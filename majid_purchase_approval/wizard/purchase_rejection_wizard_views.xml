<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        
        <!-- Purchase Rejection Wizard Form View -->
        <record id="purchase_rejection_wizard_form" model="ir.ui.view">
            <field name="name">purchase.rejection.wizard.form</field>
            <field name="model">purchase.rejection.wizard</field>
            <field name="arch" type="xml">
                <form string="Reject Purchase Order">
                    <group>
                        <field name="purchase_order_id" readonly="1"/>
                        <field name="rejected_by" readonly="1"/>
                        <field name="rejection_reason" placeholder="Masukkan alasan rejection..." required="1"/>
                    </group>
                    <footer>
                        <button name="action_reject" type="object" string="Reject" class="btn-danger"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>
        
    </data>
</odoo> 